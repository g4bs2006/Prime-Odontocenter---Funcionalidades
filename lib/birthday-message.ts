export function birthdayMessage(name: string, schedulingLink: string): string {
  return `🎂 Feliz aniversário, ${name}! A equipe Prime Odontocenter deseja a você um dia incrível. Que tal aproveitar esse dia especial e agendar sua avaliação gratuita? 😊 ${schedulingLink}`
}
